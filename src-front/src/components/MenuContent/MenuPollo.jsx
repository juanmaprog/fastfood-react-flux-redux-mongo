import React from 'react';
import PropTypes from 'prop-types';

class MenuPollo extends React.Component {
    render () {
        const {handleMenuPollo} = this.props

        return (
            <div>
                
            </div>
        )
    }
}

MenuPollo.PropTypes = {
    handleMenuPollo: PropTypes.func.isRequired
}

export default MenuPollo